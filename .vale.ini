StylesPath = styles

MinAlertLevel = suggestion
[*.{md,mdx}]

BasedOnStyles = OpenLM

; Docusaurus compiles .md as MDX, so inline Vale directives must use JSX
; comment syntax ({/* vale Style.Rule = NO */}) instead of HTML comments.
CommentDelimiters = {/*, */}

[formats]
mdx = md
