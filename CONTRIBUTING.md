# Contributing

How to add, edit, and ship documentation in this repo.

For setup and the command list, see the [README](./README.md). For deeper architecture, see [`project-guides/architecture.md`](./project-guides/architecture.md).

## Picking the right doc set

| You're documenting… | Put it in |
| --- | --- |
| Current OpenLM Platform (cloud) | `docs/cloud/<section>/` |
| Version 25 / older on-prem products (Workstation Agent, Broker, SLM, Identity Service, etc.) | `docs/legacy/<section>/` |
| The site itself, internal workflows | `project-guides/` |

There is no `docs/onpremise/` set wired into the site; older on-prem material lives in `docs/legacy/`.

## File format and frontmatter

Use `.md` for plain Markdown, `.mdx` when you need to import React components.

Standard frontmatter:

```md
---
title: Verify your setup
sidebar_position: 9
description: Confirm that Broker, Workstation Agent, and DSA are connected and that license usage data is flowing.
---

# Verify your setup
```

- `title` — page `<title>` and the H1 fallback. Always required.
- `description` — populates `<meta name="description">` and shows up in Algolia results. Write it for someone scanning search hits.
- `sidebar_position` — only meaningful if the page sits in an autogenerated section; otherwise listing order is fixed by `sidebars-cloud.js`.
- `slug` — only set when the URL must differ from the file path (also keep the i18n copy's `slug` identical).

Always write a top-level `# Heading` even though Docusaurus also renders `title` — it's the heading users see.

## Sidebars

There are two sidebar files at the repo root:

- **`sidebars-cloud.js`** — curated. Top-level order is fixed by hand. Most cloud sections are then `autogenerated` from disk, so adding a new file inside a section just appears.
- **`sidebars-default.js`** — fully autogenerated. Drives `docs/legacy/`.

When you add content:

- **New file inside an existing section** → no sidebar change needed; ordering is `_category_.json` `position` + the file's `sidebar_position`.
- **New top-level section in `docs/cloud/`** → also add a `section('Label', 'dir-name')` entry to `sidebars-cloud.js` in the right user-journey slot.
- **New section in `docs/legacy/`** → no sidebar change needed (autogenerated), but add a `_category_.json` to label and order it.

`_category_.json` shape:

```json
{
  "label": "Get started",
  "position": 1,
  "link": { "type": "generated-index" }
}
```

`generated-index` gives the category a landing page at `/cloud/category/<slug>`.

## MDX components

Components live in `src/components/`. Import them with the `@site/` alias:

```mdx
import Checklist from '@site/src/components/Checklist';
import ChecklistItem from '@site/src/components/CheckListItem';

<Checklist>
  <ChecklistItem>First step</ChecklistItem>
</Checklist>
```

Frequently useful:

- `Checklist` / `CheckListItem` — interactive checklists for setup pages.
- `DocLinkCard`, `CategoryListItem`, `CategoryPreviewCard` — link cards for landing pages.
- `DeploymentCards` / `DeploymentCard` — install-method picker.
- `DeprecationBanner` — flag legacy or end-of-life pages.
- `ArcadeEmbed` — embed Arcade walkthroughs.
- `DrawioArchitectureEmbed` — render a static `.drawio` diagram from `static/`.
- `ReleaseNotesGenerator`, `SoftwareCard`, `InstallationInstructions` — purpose-built widgets, see existing usages.

Browse `src/components/` for the full list before adding a new one. Reuse first.

## Admonitions

Standard Docusaurus admonitions plus a custom `discontinued` keyword (configured in `docusaurus.config.js`):

```md
:::note
Background information.
:::

:::tip[Optional title]
Recommended approach.
:::

:::warning
Heads-up that affects the user.
:::

:::discontinued
This feature has been removed in <version>.
:::
```

## Assets

- Page-specific images → next to the page (`docs/cloud/<section>/img/foo.png`), reference relatively.
- Shared/global images → `static/img/` (referenced as `/img/foo.png`, no `static/` prefix).
- Diagrams → keep both the source (`.drawio`) and exported `.png`/`.svg` in `static/` so they can be re-edited.

## Redirects

Whenever you **rename or move a page**, add a redirect in `docusaurus.config.js` under `@docusaurus/plugin-client-redirects`. Two forms exist:

```js
// Hand-written redirect for one URL.
{ from: '/cloud/old-path', to: '/cloud/new-path' },

// Pattern-based redirect generated for every matching live URL.
createRedirects(existingPath) { ... }
```

`onBrokenLinks` is set to `warn`, not `throw`, so broken internal links won't fail the build — read `npm run build` output and act on warnings.

## Internationalization

We ship Japanese (`ja`). Workflow:

1. Make English changes in `docs/`.
2. Run `npm run write-translations -- --locale ja`. This refreshes the JSON shells under `i18n/ja/docusaurus-plugin-content-docs-{cloud,legacy}/current/` and copies the source MDX/MD if it doesn't exist there yet.
3. Translate the copies in `i18n/ja/`. Keep `id`, `slug`, and code fences identical to the source.
4. Preview with `npm run start -- --locale ja`.
5. Commit the source change and the translations together.

See [`project-guides/localization.md`](./project-guides/localization.md) for nuance.

## Style and linting

- **Vale** — `npm run vale` runs the prose linter against `docs/`. Configuration in `.vale.ini`. Address warnings before merging.
- **Tone** — write to a working sysadmin. Imperative for steps ("Install…", not "You can install…"). Use bullet lists over prose where steps are sequential.
- **Sentence case** for headings ("Verify your setup", not "Verify Your Setup").
- **Code fences** — always tag the language (`bash`, `json`, `yaml`, `mdx`).

## Pull requests

- One PR per coherent change. If you renamed pages, redirects belong in the same PR.
- Run `npm run build` locally before requesting review for non-trivial changes — broken MDX imports surface there, not in the dev server.
- Include screenshots/screencasts for UI changes (anything in `src/`, `static/`, or `i18n/ja/.../theme*`).
- Translation PRs should ship source + translations together so locales stay in sync.
- The CI on `master` runs Python a11y checks and an axe-core WCAG 2.1 AA scan; both currently warn rather than block, but fix new violations rather than letting the warning list grow.
