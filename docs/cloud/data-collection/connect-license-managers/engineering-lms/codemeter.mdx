---
title: "Codemeter"
learnMoreLink: "/cloud/data-collection/interfacing-lms/engineering-lms/codemeter"
slug: "codemeter"
description: "OpenLM supports monitoring a wide and growing variety of license managers."
---
# Codemeter

OpenLM supports monitoring a wide and growing variety of license managers. This guide explains how to configure OpenLM Server to interface with the CodeMeter license manager, monitor license usage, and collect CodeMeter statistics.

## Requirements

1. OpenLM Platform
2. OpenLM Broker v25.x or higher installed on the same machine as CodeMeter, and approve it in [Broker Hub](/cloud/data-collection/broker-hub).

## Monitoring capabilities

When using OpenLM Broker with CodeMeter, you can:

- Collect license totals, license usage data, and denials.

- Buffer communication with OpenLM Server to handle temporary network failures.

- Remotely start and stop the license manager service.

- Get license usage reports with 1-minute resolution.

- Generate denial reports. OpenLM supports only the LIMIT REACHED type. Specifically, event WB0212 (NO MORE LICENSES) in the CodeMeter log file is reported as a denial.

## Configuring through the Broker UI

If OpenLM Server is not installed on the same machine as CodeMeter, install OpenLM Broker on the CodeMeter server to query license usage locally.

Activate CodeMeter logging.

To configure Broker:

1. Open the Broker web UI: http://localhost:5090 -> License Managers tab.

1. Click Add License Manager.

1. Enter the CodeMeter port (default: 22350). From Type, select CodeMeter. Click Add.

1. In Commands, set the Executable path to the location of CMU32.exe (typically C:\Program Files (x86)\CodeMeter\Runtime\bin). Click Execute to test the query.

1. Go to the Vendors tab. Click Add Vendor and enter the vendor name.

1. Go to the Log Files tab. Click Add Log File. Enter the path to the CodeMeter log files (default: C:\Program Data\Codemeter\Logs).
    - Use Advanced to configure file reading frequency, data limits, encoding, and log rotation handling.
    - Turn on Watch files by pattern to track license denials.

1. Click Continue.

1. Click Save.

    ![Configure OpenLM Platform](/img/engineering-lms/codemeter/image42.png)
*Configure OpenLM Platform*

1. Log in to OpenLM Platform.

1. From Menu, go to License Servers.

    ![On left menu, select Pending Server](/img/engineering-lms/codemeter/image10.png)
*On left menu, select Pending Server*

1. On left menu, select Pending Server

1. On the list, select the CodeMeter license manager and click on Approve and Merge

    ![Verifying the configuration](/img/engineering-lms/codemeter/image3.png)
*Verifying the configuration*

1. In OpenLM Web Interface -> Menu -> License Servers Live -> Server Statistics.

1. Ensure CodeMeter appears in the list with a green status indicator.

Note: It can take up to 3 minutes for the status to update for new connections.
