---
title: "LM-X"
learnMoreLink: "/cloud/data-collection/interfacing-lms/engineering-lms/lm-x"
slug: "lm-x"
---

# LM-X

## Scope

OpenLM supports monitoring for a wide and growing variety of license managers. This guide describes how to configure OpenLM to interface with LM-X, monitor license usage, and obtain license statistics.

Install OpenLM Broker on the LM-X server to query locally and forward the data to OpenLM Server.

![Diagram: OpenLM Broker querying LM-X locally](/img/engineering-lms/lm-x/image34.png)
*Diagram: OpenLM Broker querying LM-X locally*

The OpenLM Broker is optional but recommended. Installed on the LM-X host, it queries usage locally and forwards it to OpenLM Server. Broker ensures buffered communication in case of network outages or security restrictions.

In addition, Broker can query LM-X for license request denials, providing valuable insight into license efficiency.

## Requirements

1. OpenLM Platform
2. OpenLM Broker v25.x or higher installed on the same machine as LMX, and approve it in [Broker Hub](/cloud/data-collection/broker-hub).

## Configuring via Broker UI

1. Open the Broker Web UI at http://localhost:5090/#/license-managers.

1. Under License Managers, select LM Type LMX and input the port. Click Add.

    ![Add the vendor: switch to the Vendors tab -> Add Vendor -> enter a name -> Continue. IMAGE 4](/img/engineering-lms/lm-x/image139.png)
*Add the vendor: switch to the Vendors tab -> Add Vendor -> enter a name -> Continue. IMAGE 4*

1. Add the vendor: switch to the Vendors tab -> Add Vendor -> enter a name -> Continue.

    ![Click Save.](/img/engineering-lms/lm-x/image4.png)
*Click Save.*

1. Click Save.

### Configure OpenLM Platform

1. Log in to OpenLM Platform.

1. From Menu, go to License Servers.

    ![On left menu, select Pending Server](/img/engineering-lms/lm-x/image10.png)
*On left menu, select Pending Server*

1. On left menu, select Pending Server

1. On the list, select the LM-X license manager and click on Approve and Merge

    ![Verifying the configuration](/img/engineering-lms/lm-x/image3.png)
*Verifying the configuration*

1. In OpenLM Web Interface -> Menu -> License Servers Live -> Server Statistics.

1. Ensure LM-X appears in the list with a green status indicator.

Note: It can take up to 3 minutes for the status to update for new connections.
