---
title: "IBM LUM"
slug: "ibm-lum"
description: "ライセンスの使用状況、拒否、レポートのために、OpenLM Broker を介して IBM LUM を監視するように OpenLM を設定します。"
keywords:
  - ibm
  - lum
  - license manager
  - openlm broker
---

OpenLM は、OpenLM Broker を介して IBM LUM を監視します。Broker はローカルで使用状況をクエリし、データを OpenLM Server に転送します。Java JRE が必要で、Windows と UNIX/Linux の両方のサーバーで動作します。

## はじめに

- OpenLM Platform。
- IBM LUM と同じマシンにインストールされ、[Broker Hub](/cloud/data-collection/broker-hub) で承認された OpenLM Broker v25.x 以降。
- IBM LUM ホスト上の Java JRE。

## OpenLM Broker の設定

### オプション 1 — IBM LUM を自動検出

1. Broker UI を `http://localhost:5090/` で開きます。
2. **License Managers** を開き、**Detect** を選択します。
3. IBM LUM が一覧に表示されることを確認します。
4. **Validate** を選択し、次に **Save** を選択します。

### オプション 2 — IBM LUM を手動で追加

1. Broker UI を `http://localhost:5090/` で開きます。
2. **License Managers** → **Add License Manager** を開きます。
3. **License Manager Type** から **LUM** を選択します。
4. ポート番号 (既定: `1515`) を入力し、次に **Add** を選択します。
5. Cluster トグルを設定します。

    ![Cluster トグルを設定する](/img/engineering-lms/ibm-lum/image129.png)
    *Cluster トグルを設定する*

    - **Off**: 単一サーバー。
    - **On**: 高可用性 LUM (HAL) の場合。

6. **Commands** で、`i4blt.exe` へのパスを設定します。
7. **Vendors** で、正しいベンダー名を設定します。
8. **Save** を選択します。

## OpenLM Platform で IBM LUM を承認

1. OpenLM Platform にサインインします。
2. メニューから **License Servers** → **Pending Server** を開きます。
3. IBM LUM ライセンスマネージャーを選択し、**Approve and Merge** をクリックします。

## 連携の検証

1. OpenLM Platform で **License Servers Live** → **Server Statistics** を開きます。
2. IBM LUM が緑のステータスで表示されることを確認します。

:::note
新規接続の場合、ステータスの反映まで最大 3 分かかります。
:::
