---
title: GitLab
slug: "gitlab"
description: "OpenLM で GitLab のライセンス使用状況および統計を収集するための設定手順は以下のとおりです。"
keywords:
  - gitlab
  - license manager
  - openlm broker
---

OpenLM で GitLab のライセンス使用状況および統計を収集するための設定手順は以下のとおりです。

## はじめに

- OpenLM Platform。
- Gitlab と同じマシンにインストールされ、[Broker Hub](/cloud/data-collection/broker-hub) で承認された OpenLM Broker v25.x 以降。

## 監視機能

OpenLM Broker を介した GitLab の現在の監視機能は次のとおりです。

| Report resolution | License totals | License usage | Denials reporting | Borrowed license reporting | Expiration date reporting | Multiple server redundancy |
| --- | --- | --- | --- | --- | --- | --- |
| By minute | Yes | Yes | Yes | No | Yes | No |

## OpenLM Broker の設定

![OpenLM が GitLab と連携する仕組みの図](/img/engineering-lms/gitlab/image27.png)
*OpenLM が GitLab と連携する仕組みの図*

:::note
続行する前に、OpenLM Broker をインストールし、OpenLM Server へレポートするように設定しておく必要があります。
:::
### 手動で追加
1. Broker UI を `http://localhost:5090/` で開きます。

1. **License Managers** → **Add License Manager** を開きます。

1. Gitlab を選択し、ポート番号を入力して、**Add** をクリックします。

1. **Commands** ノードで Data Inquiry を選択し、ユーザー用の GitLab API エンドポイントを入力します。例: http://FQDN:80/api/v4/users

1. C:\Program Files\OpenLM\OpenLM Broker で gitlab@80.properties ファイルを開き、ログイン情報を入力して保存します。

    ![Broker で Execute をクリックし、次に Apply をクリックします。ライセンスデータの出力が表示されることを確認します。](/img/engineering-lms/gitlab/image102.png)
*Broker で Execute をクリックし、次に Apply をクリックします。ライセンスデータの出力が表示されることを確認します。*

1. **Vendors** で、ベンダーのプレースホルダーノードを選択し、GitLab と入力して、**Apply** をクリックします。

1. **Restart Broker** をクリックします。

## OpenLM Platform で GitLab を承認

1. OpenLM Platform にサインインします。
2. メニューから **License Servers** → **Pending Server** を開きます。
3. GitLab ライセンスマネージャーを選択し、**Approve and Merge** をクリックします。

## 連携の検証

1. OpenLM Platform で **License Servers Live** → **Server Statistics** を開きます。
2. GitLab が緑のステータスで表示されることを確認します。

:::note
新規接続の場合、ステータスの反映まで最大 3 分かかります。
:::
