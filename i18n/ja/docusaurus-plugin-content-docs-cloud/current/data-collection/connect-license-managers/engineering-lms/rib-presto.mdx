---
title: "Rib Presto"
slug: "rib-presto"
description: "Follow these steps to configure OpenLM to monitor license usage and retrieve statistics from the RIB Presto License Manager."
keywords:
  - rib
  - presto
  - license manager
  - openlm broker
---
{/* TRANSLATION-NOTE: 一次翻訳（自動）。見出し・[Approve / Verify / :::note] のボイラープレートは確定済み。ベンダー固有の手順本文は人手レビューで仕上げてください。*/}

# Rib Presto

OpenLM でライセンス使用状況と次を収集するための設定手順： statistics from the RIB Presto License Manager.

## はじめに

開始前に、次が揃っていることを確認してください。

- OpenLM Platform。
- RIB Presto と同じマシンにインストールされ、[Broker Hub](/cloud/data-collection/broker-hub) で承認された OpenLM Broker v25.x 以降。

## 監視機能

RIB Presto is monitored through log file parsing. The Broker reads the log file, compiles the data, and sends it to OpenLM Server for processing.

## OpenLM Broker の設定

![Diagram of how OpenLM interfaces with the RIB Presto License Manager](/img/engineering-lms/rib-presto/image27.png)
*OpenLM と RIB Presto の連携の概要*

本手順の前提として、OpenLM Broker をインストールし、OpenLM への報告設定を完了しておきます。

### 手動で追加
1. Broker Web UI を開きます： http://localhost:5090/.

1. **License Managers** → **Add License Manager** を開きます。

    ![ドロップダウンから、 select Presto and enter any port number (for example, 888) since only log file reading is used. **Add** をクリックします。](/img/engineering-lms/rib-presto/image48.png)
    *ドロップダウンから、 select Presto and enter any port number (for example, 888) since only log file reading is used. **Add** をクリックします。*
*ドロップダウンから、 select Presto and enter any port number (for example, 888) since only log file reading is used. **Add** をクリックします。*

    ![**Vendors** タブで、 **Add Vendor** をクリックします, enter RIB Presto, and **Continue** をクリックします.](/img/engineering-lms/rib-presto/image54.png)
    ***Vendors** タブで、 **Add Vendor** をクリックします, enter RIB Presto, and **Continue** をクリックします.*
***Vendors** タブで、 **Add Vendor** をクリックします, enter RIB Presto, and **Continue** をクリックします.*

    ![Go to the Log Files tab.](/img/engineering-lms/rib-presto/image61.png)
*Log Files タブを開く*

    ![**Add Log File** をクリックします。](/img/engineering-lms/rib-presto/image83.png)
    ***Add Log File** をクリックします。*
***Add Log File** をクリックします。*

1. Browse to or manually enter the path to the RIB Presto log file.

1. From the vendor list, select RIB Presto.

1. **Save** をクリックします。

## OpenLM Platform で Rib Presto を承認

1. OpenLM Platform にサインインします。
2. メニューから **License Servers** → **Pending Server** を開きます。
3. Rib Presto ライセンスマネージャーを選択し、**Approve and Merge** をクリックします。

## 連携の検証

1. OpenLM Platform で **License Servers Live** → **Server Statistics** を開きます。
2. Rib Presto が緑のステータスで表示されることを確認します。

:::note
新規接続の場合、ステータスの反映まで最大 3 分かかります。
:::
